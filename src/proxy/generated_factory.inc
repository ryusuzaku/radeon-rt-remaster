// Generated by tools/generate_observers.py
    if(auto* e=TryMake<IDirect3DTexture9,IDirect3DTexture9Observer>(original)) return e;
    if(auto* e=TryMake<IDirect3DVolumeTexture9,IDirect3DVolumeTexture9Observer>(original)) return e;
    if(auto* e=TryMake<IDirect3DCubeTexture9,IDirect3DCubeTexture9Observer>(original)) return e;
    if(auto* e=TryMake<IDirect3D9Ex,IDirect3D9ExObserver>(original)) return e;
    if(auto* e=TryMake<IDirect3DDevice9Ex,IDirect3DDevice9ExObserver>(original)) return e;
    if(auto* e=TryMake<IDirect3DSwapChain9Ex,IDirect3DSwapChain9ExObserver>(original)) return e;
    if(auto* e=TryMake<IDirect3DBaseTexture9,IDirect3DBaseTexture9Observer>(original)) return e;
    if(auto* e=TryMake<IDirect3DVertexBuffer9,IDirect3DVertexBuffer9Observer>(original)) return e;
    if(auto* e=TryMake<IDirect3DIndexBuffer9,IDirect3DIndexBuffer9Observer>(original)) return e;
    if(auto* e=TryMake<IDirect3DSurface9,IDirect3DSurface9Observer>(original)) return e;
    if(auto* e=TryMake<IDirect3D9,IDirect3D9Observer>(original)) return e;
    if(auto* e=TryMake<IDirect3DDevice9,IDirect3DDevice9Observer>(original)) return e;
    if(auto* e=TryMake<IDirect3DStateBlock9,IDirect3DStateBlock9Observer>(original)) return e;
    if(auto* e=TryMake<IDirect3DSwapChain9,IDirect3DSwapChain9Observer>(original)) return e;
    if(auto* e=TryMake<IDirect3DResource9,IDirect3DResource9Observer>(original)) return e;
    if(auto* e=TryMake<IDirect3DVertexDeclaration9,IDirect3DVertexDeclaration9Observer>(original)) return e;
    if(auto* e=TryMake<IDirect3DVertexShader9,IDirect3DVertexShader9Observer>(original)) return e;
    if(auto* e=TryMake<IDirect3DPixelShader9,IDirect3DPixelShader9Observer>(original)) return e;
    if(auto* e=TryMake<IDirect3DVolume9,IDirect3DVolume9Observer>(original)) return e;
    if(auto* e=TryMake<IDirect3DQuery9,IDirect3DQuery9Observer>(original)) return e;
